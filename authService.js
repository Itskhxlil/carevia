/**
 * authService.js
 * Future: Doctor authentication (login, logout, token management).
 *
 * Example (to be implemented):
 *
 *   export async function login(credentials) { ... }
 *   export async function logout() { ... }
 *   export function getCurrentDoctor() {
 *     return {
 *       name: localStorage.getItem("carevia_doctor_name"),
 *       specialty: localStorage.getItem("carevia_doctor_specialty"),
 *     };
 *   }
 */

// Placeholder — no backend yet
export {};
