{
  "name": "carevia",
  "version": "1.0.0",
  "description": "Carevia — Professional Clinical Workspace (React + Node.js + MySQL)",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
